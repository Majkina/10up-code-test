/** Disabling eslint for next line as pressumably in
the future there'll be more selectors exported */

// eslint-disable-next-line import/prefer-default-export
export const getPages = (state) => state.pages;
